`.pub` files in this directory are added to the authorized_keys file in the
resulting image.

Do not delete this file as it prevents the Dockerfile COPY instruction from
failing if there are no `.pub` files.
