apiVersion: v1
kind: ConfigMap
metadata:
  name: authorized_keys_slurmctld
  namespace: {{ slurm_namespace }}
data:
  authorized_keys: |
{% for key in slurmctld_ssh_access_keys -%}
    {{ key }}
{% endfor -%}