- name: Include slurmdbd install tasks
  block:
    - ansible.builtin.include_tasks: install_slurmdbd.yml
  when: inventory_hostname in groups[rke2_servers_group_name]
  run_once: true
  tags:
    - slurm
    - slurmdbd
