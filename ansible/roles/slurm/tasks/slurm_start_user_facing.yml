---
- name: Stop and disable slurmctld
  ansible.builtin.systemd:
    name: slurmctld
    state: stopped
    enabled: false

- name: Stop and disable slurmd
  ansible.builtin.systemd:
    name: slurmd
    state: stopped
    enabled: false
