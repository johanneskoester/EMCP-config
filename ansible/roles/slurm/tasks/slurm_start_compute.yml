---
- name: Stop and disable slurmctld
  ansible.builtin.systemd:
    name: slurmctld
    state: stopped
    enabled: false

- name: Start and enable slurmd
  ansible.builtin.systemd:
    name: slurmd
    state: started
    enabled: true
